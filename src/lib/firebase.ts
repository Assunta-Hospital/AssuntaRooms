
// This file is intentionally left blank or with minimal exports
// to disconnect the application from Firebase for frontend-only testing.

// Exporting a null db object to prevent breaking changes in components that import it.
export const db = null;
